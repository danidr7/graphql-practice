type User {
    id: Int
    name: String!
    email: String!
    age: Int
    salary: Float
    vip: Boolean
    profile: Profile
    status: UserStatus
}

enum UserStatus {
	ACTIVE,
	INACTIVE,
	BLOCKED
}

type Profile {
    id: Int!
    name: String!
}